server:
  port: 8081

spring:
  application:
    name: auth-service
  profiles:
    active: local

  config:
    import: "optional:configserver:"

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

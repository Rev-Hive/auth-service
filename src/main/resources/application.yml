server:
  port: 8081

spring:
  application:
    name: auth-service

  config:
    import: "optional:configserver:"

  datasource:
    url: jdbc:mysql://mysql-db:3306/revhive_db
    username: sourav
    password: sourav@123

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: itsourav14@gmail.com
    password: pjdtveecdkcxnjqt

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka
